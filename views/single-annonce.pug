extends pug-layout/_layout
block content
    section.single-annonce
        .container
            h1 #{annonce.car.brand} #{annonce.car.model}
            code
                = JSON.stringify(annonce)